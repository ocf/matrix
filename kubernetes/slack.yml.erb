apiVersion: v1
kind: Service
metadata:
  name: slack-bridge-service
spec:
  selector:
    app: slack-bridge
  ports:
    - name: slack-bridge-port
      port: 9005
      targetPort: 9005
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-bridge
  labels:
    app: slack-bridge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slack-bridge
  template:
    metadata:
      labels:
        app: slack-bridge
    spec:
      containers:
        - name: slack-appservice-irc
          image: "halfshot/matrix-appservice-slack:develop"
          imagePullPolicy: Always
          ports:
            - containerPort: 9005
          volumeMounts:
            - name: slack-config
              mountPath: /data
              readOnly: true
      volumes:
        - name: slack-config
          secret:
            secretName: slack-config
